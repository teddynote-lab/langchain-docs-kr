---
title: Cognee
---

# CogneeRetriever

이 문서는 Cognee [retriever](/oss/langchain/retrieval) 시작하기를 도와드립니다. 모든 CogneeRetriever 기능과 구성에 대한 자세한 문서는 [API reference](https://python.langchain.com/api_reference/community/retrievers/langchain_community.retrievers.cognee.CogneeRetriever.html)를 참조하세요.

### Integration 세부사항

자체 데이터 가져오기 (즉, 사용자 정의 문서 코퍼스를 인덱싱하고 검색):

| Retriever | Self-host | Cloud offering | Package |
| :--- | :--- | :---: | :---: |
[CogneeRetriever](https://python.langchain.com/api_reference/community/retrievers/langchain_community.retrievers.cognee.CogneeRetriever.html) | ✅ | ❌ | langchain-cognee |

## Setup

cognee 기본 설정을 위해서는 OpenAI API key만 있으면 됩니다.

개별 쿼리에서 자동 추적을 받고 싶다면, 아래 주석을 해제하여 [LangSmith](https://docs.smith.langchain.com/) API key를 설정할 수 있습니다:

```python
os.environ["LANGSMITH_API_KEY"] = getpass.getpass("Enter your LangSmith API key: ")
os.environ["LANGSMITH_TRACING"] = "true"
```

### Installation

이 retriever는 `langchain-cognee` 패키지에 있습니다:

```python
pip install -qU langchain-cognee
```

```python
import nest_asyncio

nest_asyncio.apply()
```

## Instantiation

이제 retriever를 인스턴스화할 수 있습니다:

```python
from langchain_cognee import CogneeRetriever

retriever = CogneeRetriever(
    llm_api_key="sk-",  # OpenAI API Key
    dataset_name="my_dataset",
    k=3,
)
```

## Usage

문서를 추가하고, 처리한 다음, 쿼리를 실행하세요. Cognee는 쿼리와 관련된 지식을 검색하고 최종 답변을 생성합니다.

```python
# Example of adding and processing documents
from langchain_core.documents import Document

docs = [
    Document(page_content="Elon Musk is the CEO of SpaceX."),
    Document(page_content="SpaceX focuses on rockets and space travel."),
]

retriever.add_documents(docs)
retriever.process_data()

# Now let's query the retriever
query = "Tell me about Elon Musk"
results = retriever.invoke(query)

for idx, doc in enumerate(results, start=1):
    print(f"Doc {idx}: {doc.page_content}")
```

## API reference

TODO: API reference 링크를 추가하세요.