---
title: AI21LLM
---

:::caution 이 서비스는 더 이상 사용되지 않습니다.
업데이트된 ChatAI21 객체는 [이 페이지](https://python.langchain.com/docs/integrations/chat/ai21/)를 참조하세요. :::

이 예제는 LangChain을 사용하여 `AI21` Jurassic 모델과 상호작용하는 방법을 다룹니다. Jamba 모델을 사용하려면 [ChatAI21 객체](https://python.langchain.com/docs/integrations/chat/ai21/)를 대신 사용하세요.

[LangChain의 AI21 모델 및 도구 전체 목록을 확인하세요.](https://pypi.org/project/langchain-ai21/)

## Installation

```python
!pip install -qU langchain-ai21
```

## Environment Setup

[AI21 API key](https://docs.ai21.com/)를 받아서 `AI21_API_KEY` environment variable을 설정해야 합니다:

```python
import os
from getpass import getpass

if "AI21_API_KEY" not in os.environ:
    os.environ["AI21_API_KEY"] = getpass()
```

## Usage

```python
from langchain_ai21 import AI21LLM
from langchain_core.prompts import PromptTemplate

template = """Question: {question}

Answer: Let's think step by step."""

prompt = PromptTemplate.from_template(template)

model = AI21LLM(model="j2-ultra")

chain = prompt | model

chain.invoke({"question": "What is LangChain?"})
```

```output
'\nLangChain is a (database)\nLangChain is a database for storing and processing documents'
```

# AI21 Contextual Answer

AI21의 contextual answers 모델을 사용하여 context로 제공되는 텍스트나 문서와 질문을 받아서 이 context에 전적으로 기반한 답변을 반환할 수 있습니다.

이는 질문에 대한 답변이 문서에 없는 경우, 모델이 이를 표시한다는 것을 의미합니다 (잘못된 답변을 제공하는 대신).

```python
from langchain_ai21 import AI21ContextualAnswers

tsm = AI21ContextualAnswers()

response = tsm.invoke(input={"context": "Your context", "question": "Your question"})
```

또한 chains, output parsers, vector DBs와 함께 사용할 수도 있습니다.

```python
from langchain_ai21 import AI21ContextualAnswers
from langchain_core.output_parsers import StrOutputParser

tsm = AI21ContextualAnswers()
chain = tsm | StrOutputParser()

response = chain.invoke(
    {"context": "Your context", "question": "Your question"},
)
```

---

<Callout icon="pen-to-square" iconType="regular">
    [Edit the source of this page on GitHub.](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/llms/ai21.mdx)
</Callout>
<Tip icon="terminal" iconType="regular">
    [Connect these docs programmatically](/use-these-docs) to Claude, VSCode, and more via MCP for    real-time answers.
</Tip>
