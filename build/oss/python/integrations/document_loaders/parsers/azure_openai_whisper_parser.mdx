---
title: Azure OpenAI Whisper Parser
---

>[Azure OpenAI Whisper Parser](https://learn.microsoft.com/en-us/azure/ai-services/speech-service/whisper-overview)는 Azure OpenAI Whisper API를 감싸는 wrapper로, 머신러닝을 활용하여 오디오 파일을 영어 텍스트로 변환합니다.
>
>이 Parser는 `.mp3`, `.mp4`, `.mpeg`, `.mpga`, `.m4a`, `.wav`, `.webm` 형식을 지원합니다.

현재 구현은 LangChain의 핵심 원칙을 따르며, 다른 loader와 함께 사용하여 오디오 다운로드와 파싱을 모두 처리할 수 있습니다. 이러한 결과로 parser는 `Iterator[Document]`를 `yield`합니다.

## Prerequisites

이 서비스는 Azure 자격 증명, Azure endpoint 및 Whisper Model deployment가 필요하며, [여기](https://learn.microsoft.com/en-us/azure/ai-services/openai/whisper-quickstart?tabs=command-line%2Cpython-new%2Cjavascript&pivots=programming-language-python) 가이드를 따라 설정할 수 있습니다. 또한 필수 종속성을 설치해야 합니다.

```python
pip install -Uq  langchain langchain-community openai
```

## Example 1

`AzureOpenAIWhisperParser`의 메서드인 `.lazy_parse`는 변환할 파일의 경로를 포함하는 `Blob` 객체를 매개변수로 받습니다.

```python
from langchain_core.documents.base import Blob

audio_path = "path/to/your/audio/file"
audio_blob = Blob(path=audio_path)
```

```python
from langchain_community.document_loaders.parsers.audio import AzureOpenAIWhisperParser

endpoint = "<your_endpoint>"
key = "<your_api_key"
version = "<your_api_version>"
name = "<your_deployment_name>"

parser = AzureOpenAIWhisperParser(
    api_key=key, azure_endpoint=endpoint, api_version=version, deployment_name=name
)
```

```python
documents = parser.lazy_parse(blob=audio_blob)
```

```python
for doc in documents:
    print(doc.page_content)
```

## Example 2

`AzureOpenAIWhisperParser`는 `GenericLoader`와 함께 `YoutubeAudioLoader`와 같은 오디오 loader와 결합하여 사용할 수도 있습니다.

```python
from langchain_community.document_loaders.blob_loaders.youtube_audio import (
    YoutubeAudioLoader,
)
from langchain_community.document_loaders.generic import GenericLoader
```

```python
# Must be a list
url = ["www.youtube.url.com"]

save_dir = "save/directory/"
```

```python
name = "<your_deployment_name>"

loader = GenericLoader(
    YoutubeAudioLoader(url, save_dir), AzureOpenAIWhisperParser(deployment_name=name)
)

docs = loader.load()
```

```python
for doc in documents:
    print(doc.page_content)
```

---

<Callout icon="pen-to-square" iconType="regular">
    [Edit the source of this page on GitHub.](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/document_loaders/parsers/azure_openai_whisper_parser.mdx)
</Callout>
<Tip icon="terminal" iconType="regular">
    [Connect these docs programmatically](/use-these-docs) to Claude, VSCode, and more via MCP for    real-time answers.
</Tip>
