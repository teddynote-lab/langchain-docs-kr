---
title: RedisStore
---

이 문서는 Redis [key-value stores](/oss/python/integrations/stores)를 시작하는 데 도움이 됩니다. 모든 `RedisStore` 기능과 구성에 대한 자세한 문서는 [API reference](https://python.langchain.com/api_reference/community/storage/langchain_community.storage.redis.RedisStore.html)를 참조하세요.

## Overview

`RedisStore`는 Redis 인스턴스에 모든 것을 저장하는 `ByteStore`의 구현체입니다.

### Integration details

| Class | Package | Local | [JS support](https://js.langchain.com/docs/integrations/stores/ioredis_storage) | Downloads | Version |
| :--- | :--- | :---: | :---: |  :---: | :---: |
| [RedisStore](https://python.langchain.com/api_reference/community/storage/langchain_community.storage.redis.RedisStore.html) | [langchain-community](https://python.langchain.com/api_reference/community/index.html) | ✅ | ✅ | ![PyPI - Downloads](https://img.shields.io/pypi/dm/langchain_community?style=flat-square&label=%20) | ![PyPI - Version](https://img.shields.io/pypi/v/langchain_community?style=flat-square&label=%20) |

## Setup

Redis byte store를 생성하려면 Redis 인스턴스를 설정해야 합니다. 로컬에서 또는 프로바이더를 통해 설정할 수 있습니다 - 옵션에 대한 개요는 [Redis 가이드](/oss/python/integrations/providers/redis)를 참조하세요.

### Installation

LangChain `RedisStore` integration은 `langchain-community` 패키지에 포함되어 있습니다:

```python
pip install -qU langchain-community redis
```

## Instantiation

이제 byte store를 인스턴스화할 수 있습니다:

```python
from langchain_community.storage import RedisStore

kv_store = RedisStore(redis_url="redis://localhost:6379")
```

## Usage

`mset` 메서드를 사용하여 다음과 같이 key 아래에 데이터를 설정할 수 있습니다:

```python
kv_store.mset(
    [
        ["key1", b"value1"],
        ["key2", b"value2"],
    ]
)

kv_store.mget(
    [
        "key1",
        "key2",
    ]
)
```

```output
[b'value1', b'value2']
```

그리고 `mdelete` 메서드를 사용하여 데이터를 삭제할 수 있습니다:

```python
kv_store.mdelete(
    [
        "key1",
        "key2",
    ]
)

kv_store.mget(
    [
        "key1",
        "key2",
    ]
)
```

```output
[None, None]
```

## API reference

모든 `RedisStore` 기능과 구성에 대한 자세한 문서는 API reference를 참조하세요: [python.langchain.com/api_reference/community/storage/langchain_community.storage.redis.RedisStore.html](https://python.langchain.com/api_reference/community/storage/langchain_community.storage.redis.RedisStore.html)

---

<Callout icon="pen-to-square" iconType="regular">
    [Edit the source of this page on GitHub.](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/stores/redis.mdx)
</Callout>
<Tip icon="terminal" iconType="regular">
    [Connect these docs programmatically](/use-these-docs) to Claude, VSCode, and more via MCP for    real-time answers.
</Tip>
