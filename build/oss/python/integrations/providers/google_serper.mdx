---
title: Serper - Google Search API
---

이 페이지는 LangChain 내에서 [Serper](https://serper.dev) Google Search API를 사용하는 방법을 다룹니다. Serper는 Google Search의 answer box, knowledge graph, organic results 데이터를 추가하는 데 사용할 수 있는 저비용 Google Search API입니다.
이 문서는 설정과 특정 Google Serper wrapper에 대한 참조, 두 부분으로 나뉩니다.

## Setup

- [serper.dev](https://serper.dev)로 이동하여 무료 계정에 가입하세요
- API key를 받아서 환경 변수(`SERPER_API_KEY`)로 설정하세요

## Wrappers

### Utility

이 API를 감싸는 GoogleSerperAPIWrapper utility가 존재합니다. 이 utility를 import하려면:

```python
from langchain_community.utilities import GoogleSerperAPIWrapper
```

Self Ask chain의 일부로 사용할 수 있습니다:

```python
from langchain_community.utilities import GoogleSerperAPIWrapper
from langchain_openai import OpenAI
from langchain.agents import initialize_agent, Tool
from langchain.agents import AgentType

import os

os.environ["SERPER_API_KEY"] = ""
os.environ['OPENAI_API_KEY'] = ""

llm = OpenAI(temperature=0)
search = GoogleSerperAPIWrapper()
tools = [
    Tool(
        name="Intermediate Answer",
        func=search.run,
        description="useful for when you need to ask with search"
    )
]

self_ask_with_search = initialize_agent(tools, llm, agent=AgentType.SELF_ASK_WITH_SEARCH, verbose=True)
self_ask_with_search.run("What is the hometown of the reigning men's U.S. Open champion?")
```

#### Output
```
Entering new AgentExecutor chain...
 Yes.
Follow up: Who is the reigning men's U.S. Open champion?
Intermediate answer: Current champions Carlos Alcaraz, 2022 men's singles champion.
Follow up: Where is Carlos Alcaraz from?
Intermediate answer: El Palmar, Spain
So the final answer is: El Palmar, Spain

> Finished chain.

'El Palmar, Spain'
```

이 wrapper에 대한 더 자세한 안내는 [이 노트북](/oss/python/integrations/tools/google_serper)을 참조하세요.

### Tool

이 wrapper를 Tool로 쉽게 로드할 수도 있습니다(Agent와 함께 사용하기 위해).
다음과 같이 할 수 있습니다:
```python
from langchain_community.agent_toolkits.load_tools import load_tools
tools = load_tools(["google-serper"])
```

tool에 대한 자세한 정보는 [이 페이지](/oss/python/integrations/tools)를 참조하세요.

---

<Callout icon="pen-to-square" iconType="regular">
    [Edit the source of this page on GitHub.](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/providers/google_serper.mdx)
</Callout>
<Tip icon="terminal" iconType="regular">
    [Connect these docs programmatically](/use-these-docs) to Claude, VSCode, and more via MCP for    real-time answers.
</Tip>
