---
title: ArxivRetriever
---

`arXiv Retriever`는 사용자가 학술 논문을 위해 arXiv 데이터베이스를 쿼리할 수 있게 해줍니다. 전체 문서 검색(PDF 파싱)과 요약 기반 검색을 모두 지원합니다.

모든 ArxivRetriever 기능 및 구성에 대한 자세한 문서는 [API reference](https://api.js.langchain.com/classes/_langchain_community.retrievers_arxiv.ArxivRetriever.html)를 참조하세요.

## Features

- Query Flexibility: 자연어 쿼리 또는 특정 arXiv ID를 사용하여 검색합니다.
- Full-Document Retrieval: PDF를 가져와서 파싱하는 옵션입니다.
- Summaries as Documents: 더 빠른 결과를 위해 요약을 검색합니다.
- Customizable Options: 최대 결과 수와 출력 형식을 구성합니다.

## Integration details

| Retriever        | Source                       | Package                                                                      |
| ---------------- | ---------------------------- | ---------------------------------------------------------------------------- |
| `ArxivRetriever` | Academic articles from arXiv | [`@langchain/community`](https://www.npmjs.com/package/@langchain/community) |

## Setup

다음 의존성이 설치되어 있는지 확인하세요:

- PDF 파싱을 위한 `pdf-parse`
- arXiv API의 XML 응답 파싱을 위한 `fast-xml-parser`

```
npm install pdf-parse fast-xml-parser
```

## Instantiation

```typescript
const retriever = new ArxivRetriever({
  getFullDocuments: false, // Set to true to fetch full documents (PDFs)
  maxSearchResults: 5, // Maximum number of results to retrieve
});
```

## Usage

`invoke` 메서드를 사용하여 관련 논문을 arXiv에서 검색합니다. 자연어 쿼리 또는 특정 arXiv ID를 사용할 수 있습니다.

```typescript
const query = "quantum computing";

const documents = await retriever.invoke(query);
documents.forEach((doc) => {
  console.log("Title:", doc.metadata.title);
  console.log("Content:", doc.pageContent); // Parsed PDF content
});
```

## Use within a chain

다른 retriever와 마찬가지로 `ArxivRetriever`는 chain을 통해 LLM 애플리케이션에 통합될 수 있습니다. 아래는 chain 내에서 retriever를 사용하는 예시입니다:

```typescript
import { ChatOpenAI } from "@langchain/openai";
import { ChatPromptTemplate } from "@langchain/core/prompts";
import {
  RunnablePassthrough,
  RunnableSequence,
} from "@langchain/core/runnables";
import { StringOutputParser } from "@langchain/core/output_parsers";
import type { Document } from "@langchain/core/documents";

const llm = new ChatOpenAI({
  model: "gpt-4o-mini",
  temperature: 0,
});

const prompt = ChatPromptTemplate.fromTemplate(`
Answer the question based only on the context provided.

Context: {context}

Question: {question}`);

const formatDocs = (docs: Document[]) => {
  return docs.map((doc) => doc.pageContent).join("\n\n");
};

const ragChain = RunnableSequence.from([
  {
    context: retriever.pipe(formatDocs),
    question: new RunnablePassthrough(),
  },
  prompt,
  llm,
  new StringOutputParser(),
]);

await ragChain.invoke("What are the latest advances in quantum computing?");
```

## API reference

모든 ArxivRetriever 기능 및 구성에 대한 자세한 문서는 [API reference](https://api.js.langchain.com/classes/_langchain_community.retrievers_arxiv.ArxivRetriever.html)를 참조하세요.

## Related

- [Retrieval guide](/oss/javascript/langchain/retrieval)

---

<Callout icon="pen-to-square" iconType="regular">
    [Edit the source of this page on GitHub.](https://github.com/langchain-ai/docs/edit/main/src/oss/javascript/integrations/retrievers/arxiv-retriever.mdx)
</Callout>
<Tip icon="terminal" iconType="regular">
    [Connect these docs programmatically](/use-these-docs) to Claude, VSCode, and more via MCP for    real-time answers.
</Tip>
