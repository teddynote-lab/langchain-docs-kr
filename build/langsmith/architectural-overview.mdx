---
title: 아키텍처 개요
sidebarTitle: 개요
---

<Info>
Self-hosted LangSmith는 가장 규모가 크고 보안을 중요시하는 고객을 위해 설계된 Enterprise Plan의 추가 기능입니다. 자세한 내용은 [가격 페이지](https://www.langchain.com/pricing)를 참조하고, 사용자 환경에서 LangSmith를 체험하기 위한 라이선스 키를 받으려면 [영업팀에 문의](https://www.langchain.com/contact-sales)하세요.
</Info>

사용자가 제어하는 클라우드 환경에서 Kubernetes(권장) 또는 Docker로 LangSmith를 실행할 수 있습니다. LangSmith 애플리케이션은 LangSmith 서버와 stateful 서비스를 포함한 여러 구성 요소로 이루어져 있습니다:

- [LangSmith frontend](#langsmith-frontend)
- [LangSmith backend](#langsmith-backend)
- [LangSmith platform backend](#langsmith-platform-backend)
- [LangSmith Playground](#langsmith-playground)
- [LangSmith queue](#langsmith-queue)
- [LangSmith ACE (Arbitrary Code Execution) backend](#langsmith-acearbitrary-code-execution-backend)
- [ClickHouse](#clickhouse)
- [PostgreSQL](#postgresql)
- [Redis](#redis)
- [Blob storage](#blob-storage) (선택 사항이지만 권장)

<div style={{ textAlign: 'center' }}>
<img
    className="block dark:hidden"
    src="/langsmith/images/cloud-arch-light.png"
    alt="라이트 모드 개요"
/>

<img
    className="hidden dark:block"
    src="/langsmith/images/cloud-arch-dark.png"
    alt="다크 모드 개요"
/>
</div>

LangSmith UI에 접근하고 API 요청을 보내려면 [LangSmith frontend](#langsmith-frontend) 서비스를 노출해야 합니다. 설치 방법에 따라 load balancer 또는 호스트 머신에 노출된 포트가 될 수 있습니다.

## Storage Services

<Note>
LangSmith Self-Hosted는 기본적으로 모든 storage 서비스를 번들로 제공합니다. 모든 storage 서비스의 외부 버전을 사용하도록 LangSmith를 구성할 수 있습니다. 프로덕션 환경에서는 **외부 storage 서비스 사용을 강력히 권장합니다**.
</Note>

### ClickHouse

[ClickHouse](https://clickhouse.com/docs/en/intro)는 온라인 분석 처리(OLAP)를 위한 고성능 컬럼 지향 SQL 데이터베이스 관리 시스템(DBMS)입니다.

LangSmith는 ClickHouse를 trace와 feedback(대용량 데이터)의 주요 데이터 저장소로 사용합니다.

### PostgreSQL

[PostgreSQL](https://www.postgresql.org/about/)은 SQL 언어를 사용하고 확장하며, 가장 복잡한 데이터 워크로드를 안전하게 저장하고 확장할 수 있는 많은 기능을 결합한 강력한 오픈 소스 객체 관계형 데이터베이스 시스템입니다.

LangSmith는 PostgreSQL을 트랜잭션 워크로드와 운영 데이터(trace와 feedback을 제외한 거의 모든 것)의 주요 데이터 저장소로 사용합니다.

### Redis

[Redis](https://github.com/redis/redis)는 디스크에 지속되는 강력한 인메모리 key-value 데이터베이스입니다. 데이터를 메모리에 보관함으로써 Redis는 캐싱과 같은 작업에 높은 성능을 제공합니다.

LangSmith는 Redis를 큐잉 및 캐싱 작업을 지원하는 데 사용합니다.

### Blob storage

LangSmith는 [AWS S3](https://aws.amazon.com/s3/), [Azure Blob Storage](https://azure.microsoft.com/en-us/services/storage/blobs/), [Google Cloud Storage](https://cloud.google.com/storage)를 포함한 여러 blob storage 제공업체를 지원합니다.

LangSmith는 blob storage를 사용하여 trace artifact, feedback 첨부 파일 및 기타 대용량 데이터 객체와 같은 대용량 파일을 저장합니다. Blob storage는 선택 사항이지만 프로덕션 배포에 강력히 권장됩니다.

## Services

### LangSmith frontend

Frontend는 Nginx를 사용하여 LangSmith UI를 제공하고 API 요청을 다른 서버로 라우팅합니다. 이는 애플리케이션의 진입점 역할을 하며 사용자에게 노출되어야 하는 유일한 구성 요소입니다.

### LangSmith backend

Backend는 CRUD API 요청의 주요 진입점이며 애플리케이션의 비즈니스 로직 대부분을 처리합니다. 여기에는 frontend와 SDK의 요청 처리, 수집을 위한 trace 준비, hub API 지원이 포함됩니다.

### LangSmith queue

Queue는 들어오는 trace와 feedback을 처리하여 비동기적으로 trace 및 feedback 데이터 저장소에 수집되고 지속되도록 보장하며, 데이터 무결성 검사를 처리하고 데이터 저장소에 성공적으로 삽입되도록 보장하며, 데이터베이스 오류나 데이터베이스에 일시적으로 연결할 수 없는 상황에서 재시도를 처리합니다.

### LangSmith platform backend

Platform backend는 주로 인증, run 수집 및 기타 대용량 작업을 처리하는 또 다른 중요한 서비스입니다.

### LangSmith playground

Playground는 LangSmith Playground 기능을 지원하기 위해 다양한 LLM API로 요청을 전달하는 서비스입니다. 이는 사용자 자신의 커스텀 모델 서버에 연결하는 데에도 사용할 수 있습니다.

### LangSmith ACE (Arbitrary Code Execution) backend

ACE backend는 보안 환경에서 임의의 코드를 실행하는 것을 처리하는 서비스입니다. 이는 LangSmith 내에서 커스텀 코드 실행을 지원하는 데 사용됩니다.

---

<Callout icon="pen-to-square" iconType="regular">
    [Edit the source of this page on GitHub.](https://github.com/langchain-ai/docs/edit/main/src/langsmith/architectural-overview.mdx)
</Callout>
<Tip icon="terminal" iconType="regular">
    [Connect these docs programmatically](/use-these-docs) to Claude, VSCode, and more via MCP for    real-time answers.
</Tip>
